## Adding data to localStorage (storing state of menu expended or collapsed)

Code added in welcome.blade.php under comment 
//code added for stoting the state of side menu in local storage so after refreshing the page the state of the side menu should remain the same

In this commit we actually performed an action of storing a value in localStorage and then getting that value, this can be used to maintain the state of a sidebar to be expended or callapsed event after a page refresh.
