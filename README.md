## On change calling api 
### *table inline editing*
* suppose we have a select input, when we change its option an api will be called and can perform actions in database


## :selected has to be used for comparing values to select an option
` <option :selected="value1 == value2"> Option </> `
